# 更新日志

这里记的是**用户能看到的变化**，倒序排列。历史更细的改动见 git 提交记录。

## 0.2.0

### 修复

- **双击「写入」只写一次**：以前第一次写入成功、第二次拿到设置层的冲突，于是你看到「写入失败（请关闭这张卡片再打开，重试一次）」——而那次写入其实成功了。现在保存状态是**按行**的，重复点击直接忽略，按钮同时置灰兜底。
- **写一行不再带走别的行**：写入前会重读一次当前配置再合并，别的行还没写入的改动、以及插件不管的字段都原样保留；写入成功后，这一行的写入结果会留在原地，不会顺手把整张卡片刷新掉。
- **只读的部署有明确说法**：设置页本身不允许写的地方，点「写入」会告诉你「这张卡片是只读的，无法写入」，而不是把设置层的错误原样抛出来。
- **新加/改名的模型行立刻有结论**：卡片开着时输入 id 也会重新问一次目录（以前要等卡片别处发生 DOM 变化，改名的行更糟）。
- **控件出错不再带走整张卡片**：某一行的控件渲染失败时，只有那一行显示「此行的元数据控件加载失败，请刷新页面」，同卡片其它行照常；整份界面数据读不到时，卡片上写一行原因，而不是一片空白。
- **运行中新增的模型会被补全**：卡片开着点「添加模型」、或保存后重新打开，都立刻生效，不用重启。
- **写错的开关不再"猜"**：取值不在文档里时会在日志里报一次，然后落到保守的那一端——`DSH_PI_AI_CATALOG_FALLBACK_INPUT` 认不出算 `off`（以前会当成"可以收图"，代价是整轮对话失败）、`DSH_PI_AI_CATALOG_REFRESH_ON_START` 认不出算 `off`、`DSH_PI_AI_CATALOG_FALLBACK` 认不出算 `full`。
- **小时数只认普通的非负十进制**：`0x10` 这种写法会被拒绝并按默认值处理，不再被读成 `0`（那等于关掉自动更新）。
- **下载失败不再被反复重试**：自动触发的两次尝试之间至少隔 5 分钟。以前网络不通时，每打开一次界面都可能再发一次约 5 MB 的请求。
- **数据没变就不重新下载**：请求带上本地存的 ETag，models.dev 回 304 时只重新盖一次时间戳。
- **同一个模型名有多家 provider 时不再互相盖掉**：models.dev 里重复的 id 都会保留下来参与排序，不会只剩先读到的那一行。
- **匹配顺序更合理**：models.dev 那一层现在优先"这个模型自家厂商发布的行"，其次"id 正好等于裸名的行"，最后才是不像聊天模型的端点（图片、embedding、语音）；这类端点只是排在后面，不会让能匹配的名字变成「无匹配」。
- **装饰后缀只朝一个方向放宽**：目录里的名字可以多带 `-exp` 这类后缀，你自己写的名字多带则不算同一个（`gpt-4o-free` 不再是 `gpt-4o`）。
- **坏掉的目录数据不再安静**：读不出来时受影响的模型照旧读作「无匹配」，但日志里会说一次（以前每次解析都可能报一次，也可能什么都不说）。
- **容量显示**：1048576 这类整 MiB 的窗口现在读作 `1M`，不再显示成 `1024K`。

### 改进

- **界面支持中英双语**：推理等级、视觉、结论行和写入提示都跟随设置页的语言（简体中文是键集来源，英文已提供），不再固定中文。
- **新增推理等级开关 `DSH_PI_AI_CATALOG_FALLBACK_LEVELS`**：`on`（默认）谁匹配上都能声明等级，`bundled` 只让自带目录声明（models.dev 只补容量），`off` 完全不碰推理；写错的取值算 `bundled`。存在的原因是镜像站常常只给一个裸的"会推理"标记，把它扩成五个等级可能给端点送去它当场拒绝的那个。
- **新增界面数据开关**：`DSH_PI_AI_CATALOG_PANEL=off` 可以完全不注册界面数据路由（补全本身不受影响）；`DSH_PI_AI_CATALOG_PANEL_HOSTS` 可以限定只在列出的 Host 上回应，跨站浏览器请求一律拒绝。
- **模型名规则集中到一处**：`lib/names.mjs` 是宿主端、刷新 CLI 和测试共用的唯一实现，不会这边修好、那边还是旧规则。
- **npm 包内容补齐**：`files` 现在也发 `lib/names.mjs`，以及 `install-plugin.mjs`、`packaging.mjs`、`dev-paths.mjs`，所以"复制文件"装法在 npm 装好的副本里也能用；`CHANGELOG.md` 也一起发。
- **`--purge` 就是"卸载并删除"**：它不再顺手把插件装回来，和它冲突的其它动作会被当成用法错误拒绝；`--update` 等同 `--apply`，跑完会告诉你只需刷新页面还是必须重启。
- **刷新 CLI 更像个工具**：`--help` 列出全部选项；缺值或认不出的参数会被拒绝，不再悄悄用默认值把数据写到别处；`--timeout-ms` 写错不会再变成一次"网络失败"；`--settings` 的覆盖报告改用和运行时同一套名字规则，不会再把你明明匹配得上的模型报成"还没覆盖"；写文件改成"先写临时文件再改名"，中断不会留下半个快照。
- **测试与打包自检**：各套件共用一套脚手架，每个套件都打印 `N/M assertions passed`，`verify.mjs` 还会检查"套件一个断言都没做就退出 0""插件 import 的模块没被发布""有开关没写进 README""README 的后缀列表和实现不一致"。
- **CI**：Node 22 / 24 上先跑不需要 DSH 安装的套件，再装一份公开的 `@deepseek-ai/dsh` 跑完整回归。

### 文档

- README 补上全部开关：默认值、全部取值，以及写错取值时的行为；新增 `DSH_CATALOG_FALLBACK_NODE_MODULES` 和 `DSH_PI_AI_SETTINGS_FILE` 的说明。
- 改正"删一行源码来去掉界面数据路由"的说法：现在是 `DSH_PI_AI_CATALOG_PANEL=off`，并把暴露面写明白——端口对别人可达的部署，别人能读到"你配置了哪些模型"；跨站浏览器请求被拒绝，可以用 Host 白名单收窄。
- 补上刷新时机的真实行为（304、失败重试的下限）、`--update` 与 `--purge` 的真实行为、`test-fallback.mjs` 需要 `--source`、更完整的套件清单与各自依赖。
- 写明白"更新插件只替换插件目录，DSH home 里的目录数据不会被碰"，以及 npm 包里到底发了哪些文件（开发工具不在包里，要源码检出）。
- 新增本文件。
